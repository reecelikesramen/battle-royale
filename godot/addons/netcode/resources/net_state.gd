@tool
class_name NetState extends Resource

# Base class for predicted-entity state snapshots.
# Subclass with @export var fields. Phase 5 wires NetSchema metadata to drive
# wire codec + reconcile channels against these fields by name.
